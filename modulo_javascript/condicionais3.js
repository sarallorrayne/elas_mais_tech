// * Operador Ternário

const idade = 21;

idade >= 18 ? console.log('Você é maior de idade') : console.log('Você é menor de idade');

// variável - condição ? o que acontece se for VERDADEIRO : o que acontece ed for FALSO

// Mais útil para retornar valores do que para console.log
const mensagem = idade >= 18 ? 'Você é maior de idade' : 'Você é menor de idade';
console.log(mensagem);